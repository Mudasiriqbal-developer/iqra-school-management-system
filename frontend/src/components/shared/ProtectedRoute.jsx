import React from 'react';

/**
 * Route protection wrapper. Currently returns children (Phase 0 placeholder).
 */
const ProtectedRoute = ({ children }) => {
  return children;
};

export default ProtectedRoute;
